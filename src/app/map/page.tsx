import React from 'react';
import LocationTracker from '../../components/LocationTracker/LocationTracker'; // Asegúrate de ajustar la ruta según tu estructura de archivos

const MapPage: React.FC = () => {
  return (
    <div>
      <LocationTracker />
    </div>
  );
};

export default MapPage;
